# DrAttack Algorithm Skill

DrAttack is reserved in Agent Mode.

Do not emulate DrAttack manually with PAIR commands. The future deterministic DrAttack controller will own decomposition, transformation/reconstruction stages, validation, iteration limits, and stop conditions.
